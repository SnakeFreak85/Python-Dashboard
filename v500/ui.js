(function(){
'use strict';
function animalCard(x){const a=x.a,esc=NGT500.esc;const lf=(a.feeds||[]).slice(-1)[0],lw=(a.weights||[]).slice(-1)[0],photo=(a.photos||[])[0];return `<div class="animal">${photo?`<img class="photo" src="${photo.data}">`:''}<h2>${esc(a.name)}</h2><span class="pill">${esc(a.morph||'-')}</span><span class="pill">${esc(a.sex||'-')}</span><span class="pill">${esc(a.status||'-')}</span><br>${a.defaultFeeder?`🍽️ Standard: <b>${esc(a.defaultFeeder)}</b><br>`:''}💵 Kauf: ${NGT500.money(a.buyPrice||0)} · Markt: ${NGT500.money(NGTStore.market(a))}<br>${lf?`🍴 Letzte Fütterung: ${esc(lf.date)} ${lf.accepted===false?'verweigert':'gefressen'}<br>`:''}${lw?`⚖️ Gewicht: ${esc(lw.weight)}g (${esc(lw.date)})<br>`:''}<div class="btnRow"><button onclick="NGT500.route('profile',{t:'${x.t}',i:${x.i}})">📁 Tierakte</button><button onclick="NGT500.route('animals',{t:'${x.t}',edit:${x.i}})">✏️ Bearbeiten</button><button onclick="NGT500.route('qr',{id:'${a.uuid}'})">📱 QR</button><button class="danger" onclick="NGTAnimals.remove('${x.t}',${x.i})">🗑️ Löschen</button></div></div>`}
function list(rows){return rows.length?rows.map(r=>`<div class="listLine"><b>${NGT500.esc(r.d||'-')}</b> ${NGT500.esc(r.txt||'')}</div>`).join(''):'<p class="muted">Keine Einträge.</p>'}
function timeline(a){let rows=[];(a.feeds||[]).forEach(f=>rows.push({d:f.date,txt:`🍴 ${f.accepted===false?'Verweigert':'Gefressen'} ${f.amount?f.amount+'g ':''}${f.prey||''}`}));(a.sheds||[]).forEach(s=>rows.push({d:s.date,txt:'🧤 Häutung'}));(a.weights||[]).forEach(w=>rows.push({d:w.date,txt:`⚖️ ${w.weight}g`}));(a.photos||[]).forEach(p=>rows.push({d:p.date,txt:`📷 Foto ${p.note||''}`}));return rows.sort((x,y)=>String(y.d).localeCompare(String(x.d)))}
window.NGTUI={animalCard,list,timeline};
})();
